# Contributing Guidelines

## Commit Guidelines

All commits must adhere to the Conventional Commits specification.

## Pull Request Lifecycle

1. Fork and create topic branch.
2. Verify all local checks pass (`npm run lint`, `npm run format:check`, `npm run test:coverage`).
3. Open a Pull Request referencing the design issue number.
