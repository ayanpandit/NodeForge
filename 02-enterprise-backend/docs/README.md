# Architecture Documentation Index

This directory serves as the documentation center for system architecture and features.

## Documenting New Features

When adding new architecture domains or layers (e.g. database setup, Redis caching, microservices messaging), add a corresponding Markdown reference file in this directory.
