# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.0.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [Unreleased]

### Added

- Scalable directory structure scaffolding.
- Express application bootstrap (`app.js`) and HTTP server setup (`server.js`).
- Port verification and environment validator loading mechanism.
- ESLint (modern flat config), Prettier configuration, and EditorConfig.
- Built-in test runner configurations and bootstrap suite.
- GitHub Actions CI workflow and Dependabot configuration.
